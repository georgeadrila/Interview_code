// comparing two numbers and outsputting the small and large number

const num1 = 10;
const num2 = 5;

if (num1 > num2) {
    console.log(`${num1} is larger than ${num2}`);
} else if (num1 < num2) {
    console.log(`${num1} is smaller than ${num2}`);
} else {
    console.log(`${num1} and ${num2} are equal`);
}
